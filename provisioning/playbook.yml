---
- hosts: srilmbox
  vars:
    lmserver_dir: "/home/vagrant/gits/language-model-server"

  tasks:
    - name: generate locale
      command: locale-gen en_US.UTF-8
      sudo: yes
  vars_files:
    - [ "vars/srilm.yml" ]
  roles:
    - { role: ronocdh-env }
    - { role: srilm }
    - { role: lmserver }

